---
description: Cadence stale-lock sweeper — clears cadence-active labels on issues with no recent activity. (Session A stub; full body lands in Session C.)
disable-model-invocation: true
---

# /cadence-sweep (Session A stub)

The stale-lock sweeper is implemented in **Session C** of the build plan.
This stub is a placeholder so the slash command exists and the plugin loads.

Print verbatim, then stop:

```
TODO: /cadence:cadence-sweep is not yet implemented.

This command will clear cadence-active labels from Linear issues that have
not been updated within limits.stale_after_minutes (default 30). Lands in
Session C per PLAN.md ("/cadence-sweep semantics").
```

Do not call any tools. Exit immediately after printing.
